import { StyleSheet } from "react-native";

export const telaheader = StyleSheet.create({
  header: {
    backgroundColor: "#afa6db",
    width: "100%",
    alignItems: "center",
  },
  logo: {
    width: 80,
    height: 80,
    resizeMode: "contain",
  },
});
