import { StyleSheet } from "react-native";

export const telaheader = StyleSheet.create({
  header: {
    backgroundColor: "#efefff",
    width: "100%",
    alignItems: "center",
  },
  logo: {
    width: 80,
    height: 80,
    resizeMode: "contain",
  },
});
