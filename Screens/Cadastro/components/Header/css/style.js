import { StyleSheet } from "react-native";

export const cadastro = StyleSheet.create({
  viewlogo: {
    alignItems: "center",
    padding: 50,
    backgroundColor: "#efefff",
  },
  logo: {
    width: 150,
    height: 150,
    resizeMode: "contain",
  },
});
